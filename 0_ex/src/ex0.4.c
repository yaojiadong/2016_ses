/*
 ============================================================================
 Name        : 4.c
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */
/*
 ============================================================================
 Name        : 4.c
 Author      :
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
/*
int main(void) {
    uint8_t var = 0;
	//Set bit 3
    var = var | (1<<3);
	printf("Set bit 3: 0x%02x\n",var);
	//Set bits 4 and 6 (with / without bit shifting)
	var = var|0x50;
	printf("Set bits 4 and 6: 0x%02x\n",var);
	//Clear bit 2
	//var = var & 0xFB;
	var = var & ~(1<<2);
	printf("Clear bit 2: 0x%02x\n",var);
	//Clear bits 2 and 7
	var = var & 0x7B;
	printf("Clear bits 2 and 7: 0x%02x\n",var);
	//Toggle (invert) bit 3
	var = var ^ (1<<3);
	printf("Toggle (invert) bit 3: 0x%02x\n",var);
	//Set bit 2 and clear bits 5 and 7 at the same time
	var = (var | (1<<2))& ~(1<<5)&~(1<<7);
	printf("Set bit 2 and clear bits 5 and 7 at the same time: 0x%02x\n",var);
	//uint8_t var1=((var<<4)|0x40)>>4;
	//uint8_t var2=((var>>4)&0xF5)<<4;
//	printf("the result is 0x%02x\n",var1);
//	printf("the result is 0x%02x\n",var2);

	//Swap bits 3-5 and bits 0-2
	uint8_t x = (var>>3 ^ var) & ((1<<3)-1);
	var = var^((x<<3)|x);
	printf("swap bits 3-5 and bits 0-2:0x%02x\n",var);
	return EXIT_SUCCESS;
}*/
